export const state = () => ({
  loading: false,
})

// export const mutations = {
//   $page(state, val) {
//     state.loading = val ? val : !state.loading
//   },
//   $api(state, val) {
//     state.api = val ? val : !state.api
//   }
// }
